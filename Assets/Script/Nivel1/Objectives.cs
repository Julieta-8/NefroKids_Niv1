using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Objectives : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //poner un cronometro para completar los más rapido posible las 2 fases
        // si la fase 1 o 2: en 25 segundos: 1 20 segundos:2 10 segundos: 3
        //en vez de monedas ir sumando estrellas



        //datos curiosos: la cantidad de minutos que se lava las manos / ¿porque? / priorizar la higiene

        // en momentos randoms tener preparados momentos de alerta: el chico tiene miedo/ le duele la panza ¿Que hacemos? --> plantear diferentes soluciones y la que eleija afecta el resultado


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
